# Credentials — never commit filled-in launcher scripts
*.bat
!gui/build.bat
*credentials*
*password*
*.key
*.pem
*.p12
*.pfx

# Build artifacts — build.bat regenerates these
gui/IntranetVpn.jar
gui/*.class
gui/build/

# Upstream binary — use SHA256 verification per README instead of committing
zju-connect.exe
*.zip

# OS
Thumbs.db
.DS_Store
desktop.ini
.aider*
